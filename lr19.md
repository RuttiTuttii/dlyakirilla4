Отлично, давайте разберем все подробно, шаг за шагом.

📊 Понимание текущей БД и ее реверс-инжиниринг

Какие таблицы нам нужны из SQL схемы:

Из файла ispp3111.sql.txt видим следующие основные таблицы для кинотеатра:

1. Film - Фильмы
2. Session - Сеансы
3. Hall - Кинозалы
4. Ticket - Билеты
5. Visitor - Посетители
6. Genre - Жанры
7. FilmGenre - Связь фильмов и жанров

НО! Для аутентификации нам нужна таблица пользователей (Users), которой нет в схеме. Есть только Visitor (посетители кинотеатра), но это разные сущности.

🔄 Шаг 1: Реверс-инжиниринг БД + добавление Users

Вариант A: Использовать Entity Framework Core для реверс-инжиниринга

```bash
# Установка инструментов
dotnet tool install --global dotnet-ef

# Создание моделей из существующей БД
dotnet ef dbcontext scaffold "Server=(localdb)\mssqllocaldb;Database=ispp3107;Trusted_Connection=True;" Microsoft.EntityFrameworkCore.SqlServer -o Models --context-dir Data --context CinemaContext --force
```

Вариант B: Вручную добавить недостающую таблицу Users

Создаем SQL скрипт для добавления таблицы пользователей:

```sql
-- Добавляем таблицу Users для аутентификации
CREATE TABLE [dbo].[Users] (
    [UserId] [int] IDENTITY(1,1) NOT NULL,
    [Username] [nvarchar](50) NOT NULL,
    [PasswordHash] [nvarchar](256) NOT NULL,
    [Role] [nvarchar](50) NOT NULL DEFAULT 'Гость',
    [Email] [nvarchar](100) NULL,
    [CreatedDate] [datetime] NOT NULL DEFAULT GETDATE(),
    CONSTRAINT [PK_Users] PRIMARY KEY ([UserId]),
    CONSTRAINT [UQ_Username] UNIQUE ([Username])
);

-- Добавляем тестовых пользователей
INSERT INTO [dbo].[Users] ([Username], [PasswordHash], [Role], [Email]) VALUES
('admin', 'AQAAAAIAAYagAAAAEJaVp6R1DM9h2VG0ufp84xPYZe8mCcrSJRJQRUKV6lH5tPESG8Pegjci8j7oJJZ8RQ==', 'Администратор', 'admin@cinema.local'),
('ticket_seller', 'AQAAAAIAAYagAAAAEJaVp6R1DM9h2VG0ufp84xPYZe8mCcrSJRJQRUKV6lH5tPESG8Pegjci8j7oJJZ8RQ==', 'Билетер', 'ticket@cinema.local'),
('guest', 'AQAAAAIAAYagAAAAEJaVp6R1DM9h2VG0ufp84xPYZe8mCcrSJRJQRUKV6lH5tPESG8Pegjci8j7oJJZ8RQ==', 'Гость', 'guest@cinema.local');
```

Пароль для всех тестовых пользователей: Password123! (захешированный)

🎯 Шаг 2: Создание проекта с правильной структурой

1. Создаем новый проект:

```bash
dotnet new webapp -n CinemaWebApp -f net8.0
cd CinemaWebApp
```

2. Добавляем необходимые NuGet пакеты:

```xml
<!-- CinemaWebApp.csproj -->
<ItemGroup>
  <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="8.0.0" />
  <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="8.0.0">
    <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    <PrivateAssets>all</PrivateAssets>
  </PackageReference>
  <PackageReference Include="Microsoft.EntityFrameworkCore.Tools" Version="8.0.0">
    <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    <PrivateAssets>all</PrivateAssets>
  </PackageReference>
  <PackageReference Include="Microsoft.AspNetCore.Mvc.Razor.RuntimeCompilation" Version="8.0.0" />
</ItemGroup>
```

3. Создаем папки в проекте:

```
CinemaWebApp/
├── Data/                 # Контекст БД и репозитории
├── Models/              # Модели сущностей
│   ├── Entities/        # Основные сущности БД
│   └── ViewModels/      # Модели для представлений
├── Pages/               # Razor Pages
│   ├── Films/
│   ├── Sessions/
│   ├── Tickets/
│   ├── Visitors/
│   ├── Account/
│   └── Shared/
├── Services/            # Сервисы бизнес-логики
├── Utilities/           # Вспомогательные классы
└── wwwroot/
```

📝 Шаг 3: Создание моделей из БД

Файл: Models/Entities/Film.cs

```csharp
using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.ComponentModel.DataAnnotations.Schema;

namespace CinemaWebApp.Models.Entities
{
    [Table("Film")]
    public partial class Film
    {
        [Key]
        [Display(Name = "ID фильма")]
        public int FilmId { get; set; }

        [Required(ErrorMessage = "Название обязательно")]
        [Display(Name = "Название")]
        [StringLength(100, ErrorMessage = "Максимальная длина 100 символов")]
        public string Title { get; set; }

        [Display(Name = "Длительность (мин)")]
        [Range(1, 500, ErrorMessage = "Длительность от 1 до 500 минут")]
        public short Duration { get; set; }

        [Display(Name = "Год выпуска")]
        [Range(1900, 2100, ErrorMessage = "Год от 1900 до 2100")]
        public short ReleaseYear { get; set; }

        [Display(Name = "Описание")]
        [DataType(DataType.MultilineText)]
        [StringLength(500, ErrorMessage = "Максимальная длина 500 символов")]
        public string Description { get; set; }

        [Display(Name = "Постер")]
        public byte[] Poster { get; set; }

        [Display(Name = "Возрастной рейтинг")]
        [StringLength(10, ErrorMessage = "Максимальная длина 10 символов")]
        public string AgeRating { get; set; }

        [Display(Name = "Начало проката")]
        [DataType(DataType.Date)]
        public DateTime? RentalStart { get; set; }

        [Display(Name = "Окончание проката")]
        [DataType(DataType.Date)]
        public DateTime? RentalEnd { get; set; }

        [Display(Name = "Удален")]
        public bool? IsDeleted { get; set; }

        // Навигационные свойства
        public virtual ICollection<Session> Sessions { get; set; }
        public virtual ICollection<FilmGenre> FilmGenres { get; set; }
    }
}
```

Файл: Models/Entities/User.cs (Новая таблица для аутентификации)

```csharp
using System.ComponentModel.DataAnnotations;

namespace CinemaWebApp.Models.Entities
{
    public class User
    {
        [Key]
        public int UserId { get; set; }

        [Required(ErrorMessage = "Имя пользователя обязательно")]
        [Display(Name = "Имя пользователя")]
        [StringLength(50, MinimumLength = 3, ErrorMessage = "Длина от 3 до 50 символов")]
        public string Username { get; set; }

        [Required(ErrorMessage = "Пароль обязателен")]
        [Display(Name = "Пароль")]
        [DataType(DataType.Password)]
        [StringLength(256)]
        public string PasswordHash { get; set; }

        [Required]
        [Display(Name = "Роль")]
        [StringLength(50)]
        public string Role { get; set; }

        [Display(Name = "Email")]
        [EmailAddress(ErrorMessage = "Некорректный email")]
        [StringLength(100)]
        public string Email { get; set; }

        [Display(Name = "Дата создания")]
        public DateTime CreatedDate { get; set; }
    }
}
```

🗄️ Шаг 4: Создание контекста БД

Файл: Data/CinemaContext.cs

```csharp
using CinemaWebApp.Models.Entities;
using Microsoft.EntityFrameworkCore;

namespace CinemaWebApp.Data
{
    public class CinemaContext : DbContext
    {
        public CinemaContext(DbContextOptions<CinemaContext> options)
            : base(options)
        {
        }

        public DbSet<Film> Films { get; set; }
        public DbSet<Session> Sessions { get; set; }
        public DbSet<Hall> Halls { get; set; }
        public DbSet<Ticket> Tickets { get; set; }
        public DbSet<Visitor> Visitors { get; set; }
        public DbSet<Genre> Genres { get; set; }
        public DbSet<FilmGenre> FilmGenres { get; set; }
        public DbSet<User> Users { get; set; }

        protected override void OnModelCreating(ModelBuilder modelBuilder)
        {
            base.OnModelCreating(modelBuilder);

            // Настройка связей
            modelBuilder.Entity<Session>()
                .HasOne(s => s.Film)
                .WithMany(f => f.Sessions)
                .HasForeignKey(s => s.FilmId)
                .OnDelete(DeleteBehavior.Restrict);

            modelBuilder.Entity<Session>()
                .HasOne(s => s.Hall)
                .WithMany(h => h.Sessions)
                .HasForeignKey(s => s.HallId)
                .OnDelete(DeleteBehavior.Restrict);

            modelBuilder.Entity<Ticket>()
                .HasOne(t => t.Session)
                .WithMany(s => s.Tickets)
                .HasForeignKey(t => t.SessionId)
                .OnDelete(DeleteBehavior.Restrict);

            modelBuilder.Entity<Ticket>()
                .HasOne(t => t.Visitor)
                .WithMany(v => v.Tickets)
                .HasForeignKey(t => t.VisitorId)
                .OnDelete(DeleteBehavior.Restrict);

            modelBuilder.Entity<FilmGenre>()
                .HasKey(fg => new { fg.FilmId, fg.GenreId });

            // Уникальные ограничения
            modelBuilder.Entity<Hall>()
                .HasIndex(h => new { h.Cinema, h.HallNumber })
                .IsUnique();

            modelBuilder.Entity<Visitor>()
                .HasIndex(v => v.Phone)
                .IsUnique();

            // Значения по умолчанию
            modelBuilder.Entity<Film>()
                .Property(f => f.IsDeleted)
                .HasDefaultValue(false);

            modelBuilder.Entity<Session>()
                .Property(s => s.Is3d)
                .HasDefaultValue(false);
        }
    }
}
```

🚀 Шаг 5: Генерация CRUD страниц

Для каждой сущности генерируем страницы:

```bash
# Генерация страниц для Films
dotnet aspnet-codegenerator razorpage -m CinemaWebApp.Models.Entities.Film -dc CinemaWebApp.Data.CinemaContext -udl -outDir Pages/Films --referenceScriptLibraries -f

# Генерация страниц для Sessions
dotnet aspnet-codegenerator razorpage -m CinemaWebApp.Models.Entities.Session -dc CinemaWebApp.Data.CinemaContext -udl -outDir Pages/Sessions --referenceScriptLibraries -f

# Генерация страниц для Tickets
dotnet aspnet-codegenerator razorpage -m CinemaWebApp.Models.Entities.Ticket -dc CinemaWebApp.Data.CinemaContext -udl -outDir Pages/Tickets --referenceScriptLibraries -f

# Генерация страниц для Visitors
dotnet aspnet-codegenerator razorpage -m CinemaWebApp.Models.Entities.Visitor -dc CinemaWebApp.Data.CinemaContext -udl -outDir Pages/Visitors --referenceScriptLibraries -f
```

🎨 Шаг 6: Кастомизация шаблонов страниц

Базовый шаблон для всех страниц с аутентификацией:

Файл: Pages/Shared/AuthPage.cshtml.cs

```csharp
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;
using Microsoft.EntityFrameworkCore;

namespace CinemaWebApp.Pages.Shared
{
    public abstract class AuthPageModel : PageModel
    {
        protected readonly CinemaContext _context;
        
        public string UserRole => HttpContext.Session.GetString("Role") ?? string.Empty;
        public string Username => HttpContext.Session.GetString("Username") ?? string.Empty;
        
        public bool IsAdmin => UserRole == "Администратор";
        public bool IsTicketSeller => UserRole == "Билетер";
        public bool IsGuest => UserRole == "Гость";
        
        public AuthPageModel(CinemaContext context)
        {
            _context = context;
        }
        
        protected IActionResult? HasRole()
        {
            if (string.IsNullOrEmpty(UserRole))
                return RedirectToPage("/Account/Login");
            return null;
        }
        
        protected IActionResult? IsInRole(string role)
        {
            if (UserRole != role)
                return RedirectToPage("/Account/Login");
            return null;
        }
        
        protected IActionResult? CanEdit()
        {
            if (!IsAdmin)
                return RedirectToPage("/Account/Login");
            return null;
        }
    }
}
```

🔧 Шаг 7: Настройка Program.cs со всеми сервисами

```csharp
using CinemaWebApp.Data;
using Microsoft.EntityFrameworkCore;
using System.Globalization;

var builder = WebApplication.CreateBuilder(args);

// Добавление сервисов
builder.Services.AddRazorPages(options =>
{
    options.Conventions.AddPageRoute("/Films/Index", "");
})
.AddRazorRuntimeCompilation();

builder.Services.AddDbContext<CinemaContext>(options =>
    options.UseSqlServer(builder.Configuration.GetConnectionString("DefaultConnection")));

builder.Services.AddSession(options =>
{
    options.IdleTimeout = TimeSpan.FromMinutes(30);
    options.Cookie.HttpOnly = true;
    options.Cookie.IsEssential = true;
});

builder.Services.AddHttpContextAccessor();

// Настройка локализации для десятичных чисел
CultureInfo.DefaultThreadCurrentCulture = new CultureInfo("ru-RU")
{
    NumberFormat = new NumberFormatInfo
    {
        NumberDecimalSeparator = ".",
        CurrencyDecimalSeparator = ".",
        CurrencySymbol = "₽"
    }
};

var app = builder.Build();

// Конфигурация конвейера запросов
if (!app.Environment.IsDevelopment())
{
    app.UseExceptionHandler("/Error");
    app.UseHsts();
}

app.UseHttpsRedirection();
app.UseStaticFiles();
app.UseRouting();
app.UseAuthorization();
app.UseSession();

app.MapRazorPages();

// Инициализация базы данных
using (var scope = app.Services.CreateScope())
{
    var context = scope.ServiceProvider.GetRequiredService<CinemaContext>();
    context.Database.EnsureCreated();
    
    // Если нет пользователей, добавляем тестовых
    if (!context.Users.Any())
    {
        context.Users.AddRange(
            new Models.Entities.User
            {
                Username = "admin",
                PasswordHash = "AQAAAAIAAYagAAAAEJaVp6R1DM9h2VG0ufp84xPYZe8mCcrSJRJQRUKV6lH5tPESG8Pegjci8j7oJJZ8RQ==",
                Role = "Администратор",
                Email = "admin@cinema.local"
            },
            new Models.Entities.User
            {
                Username = "ticket_seller",
                PasswordHash = "AQAAAAIAAYagAAAAEJaVp6R1DM9h2VG0ufp84xPYZe8mCcrSJRJQRUKV6lH5tPESG8Pegjci8j7oJJZ8RQ==",
                Role = "Билетер",
                Email = "ticket@cinema.local"
            }
        );
        context.SaveChanges();
    }
}

app.Run();
```

📋 Шаг 8: Создание страницы Login с полным функционалом

Файл: Pages/Account/Login.cshtml

```html
@page
@model CinemaWebApp.Pages.Account.LoginModel

@{
    ViewData["Title"] = "Вход в систему";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h4>Вход в систему кинотеатра</h4>
            </div>
            <div class="card-body">
                <!-- Форма входа для зарегистрированных пользователей -->
                <form method="post" asp-page-handler="Login">
                    <div class="mb-3">
                        <label asp-for="Username" class="form-label">Имя пользователя</label>
                        <input asp-for="Username" class="form-control" />
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Password" class="form-label">Пароль</label>
                        <input asp-for="Password" type="password" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Войти</button>
                </form>
                
                <hr />
                
                <!-- Форма входа как гость -->
                <form method="post" asp-page-handler="Guest">
                    <div class="mb-3">
                        <p>Войти без регистрации с ограниченными правами:</p>
                        <button type="submit" class="btn btn-outline-secondary w-100">Войти как гость</button>
                    </div>
                </form>
                
                @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                {
                    <div class="alert alert-danger mt-3">@Model.ErrorMessage</div>
                }
                
                <!-- Тестовые данные -->
                <div class="mt-4">
                    <h6>Тестовые данные:</h6>
                    <ul class="list-unstyled">
                        <li><strong>Администратор:</strong> admin / Password123!</li>
                        <li><strong>Билетер:</strong> ticket_seller / Password123!</li>
                        <li><strong>Гость:</strong> нажмите "Войти как гость"</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
```

Файл: Pages/Account/Login.cshtml.cs

```csharp
using CinemaWebApp.Data;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;
using Microsoft.EntityFrameworkCore;

namespace CinemaWebApp.Pages.Account
{
    public class LoginModel : PageModel
    {
        private readonly CinemaContext _context;

        [BindProperty]
        [Required(ErrorMessage = "Имя пользователя обязательно")]
        public string Username { get; set; }

        [BindProperty]
        [Required(ErrorMessage = "Пароль обязателен")]
        [DataType(DataType.Password)]
        public string Password { get; set; }

        [TempData]
        public string ErrorMessage { get; set; }

        public LoginModel(CinemaContext context)
        {
            _context = context;
        }

        public void OnGet()
        {
        }

        public async Task<IActionResult> OnPostLoginAsync()
        {
            if (!ModelState.IsValid)
                return Page();

            // Ищем пользователя в БД
            var user = await _context.Users
                .FirstOrDefaultAsync(u => u.Username == Username);

            if (user == null)
            {
                ErrorMessage = "Пользователь не найден";
                return Page();
            }

            // В реальном приложении здесь должно быть хеширование пароля
            // Для теста используем простую проверку
            var hasher = new PasswordHasher<User>();
            var result = hasher.VerifyHashedPassword(user, user.PasswordHash, Password);

            if (result == PasswordVerificationResult.Failed)
            {
                ErrorMessage = "Неверный пароль";
                return Page();
            }

            // Сохраняем данные в сессии
            HttpContext.Session.SetString("Role", user.Role);
            HttpContext.Session.SetString("Username", user.Username);
            HttpContext.Session.SetInt32("UserId", user.UserId);

            return RedirectToPage("/Films/Index");
        }

        public IActionResult OnPostGuest()
        {
            HttpContext.Session.Clear();
            HttpContext.Session.SetString("Role", "Гость");
            HttpContext.Session.SetString("Username", "Гость");

            return RedirectToPage("/Films/Index");
        }

        public IActionResult OnGetLogout()
        {
            HttpContext.Session.Clear();
            return RedirectToPage("/Account/Login");
        }
    }
}
```

🛠️ Шаг 9: Создание страницы Sessions с полным функционалом ЛР №19

Файл: Pages/Sessions/Index.cshtml.cs (полная версия)

```csharp
using CinemaWebApp.Data;
using CinemaWebApp.Models.Entities;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.RazorPages;
using Microsoft.AspNetCore.Mvc.Rendering;
using Microsoft.EntityFrameworkCore;
using System.Linq.Dynamic.Core;

namespace CinemaWebApp.Pages.Sessions
{
    public class IndexModel : AuthPageModel
    {
        public IndexModel(CinemaContext context) : base(context)
        {
        }

        // Свойства для поиска и фильтрации
        [BindProperty(SupportsGet = true)]
        public string FilmTitle { get; set; }

        [BindProperty(SupportsGet = true)]
        public string SortColumn { get; set; } = "StartDate";

        [BindProperty(SupportsGet = true)]
        public string SortDirection { get; set; } = "asc";

        [BindProperty(SupportsGet = true)]
        public int? HallId { get; set; }

        [BindProperty(SupportsGet = true)]
        public DateTime? StartDateFrom { get; set; }

        [BindProperty(SupportsGet = true)]
        public DateTime? StartDateTo { get; set; }

        // Пагинация
        [BindProperty(SupportsGet = true)]
        public int CurrentPage { get; set; } = 1;

        public int PageSize { get; set; } = 10;
        public int TotalItems { get; set; }
        public int TotalPages => (int)Math.Ceiling(TotalItems / (double)PageSize);

        public List<Session> Sessions { get; set; } = new();
        public SelectList HallsSelectList { get; set; }

        public async Task<IActionResult> OnGetAsync()
        {
            // Проверка роли - доступно всем
            if (HasRole() is IActionResult result) return result;

            // Получаем все сеансы с связанными данными
            var query = _context.Sessions
                .Include(s => s.Film)
                .Include(s => s.Hall)
                .AsQueryable();

            // Применяем фильтрацию
            if (!string.IsNullOrEmpty(FilmTitle))
            {
                query = query.Where(s => s.Film.Title.Contains(FilmTitle));
            }

            if (HallId.HasValue)
            {
                query = query.Where(s => s.HallId == HallId.Value);
            }

            if (StartDateFrom.HasValue)
            {
                query = query.Where(s => s.StartDate >= StartDateFrom.Value);
            }

            if (StartDateTo.HasValue)
            {
                query = query.Where(s => s.StartDate <= StartDateTo.Value);
            }

            // Применяем сортировку
            if (!string.IsNullOrEmpty(SortColumn))
            {
                var direction = SortDirection == "desc" ? "descending" : "ascending";
                query = query.OrderBy($"{SortColumn} {direction}");
            }

            // Пагинация
            TotalItems = await query.CountAsync();
            
            Sessions = await query
                .Skip((CurrentPage - 1) * PageSize)
                .Take(PageSize)
                .ToListAsync();

            // Получаем список залов для фильтра
            var halls = await _context.Halls
                .Select(h => new
                {
                    h.HallId,
                    DisplayText = $"{h.Cinema} - Зал {h.HallNumber}"
                })
                .ToListAsync();

            HallsSelectList = new SelectList(halls, "HallId", "DisplayText");

            return Page();
        }

        // Метод для изменения сортировки
        public IActionResult OnGetSort(string column)
        {
            if (SortColumn == column)
            {
                SortDirection = SortDirection == "asc" ? "desc" : "asc";
            }
            else
            {
                SortColumn = column;
                SortDirection = "asc";
            }

            return RedirectToPage(new
            {
                FilmTitle,
                SortColumn,
                SortDirection,
                HallId,
                StartDateFrom,
                StartDateTo,
                CurrentPage = 1 // Сбрасываем на первую страницу при сортировке
            });
        }

        // Метод для сброса фильтров
        public IActionResult OnGetReset()
        {
            FilmTitle = null;
            HallId = null;
            StartDateFrom = null;
            StartDateTo = null;
            SortColumn = "StartDate";
            SortDirection = "asc";
            CurrentPage = 1;

            return RedirectToPage();
        }
    }
}
```

📊 Шаг 10: Настройка appsettings.json

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=ispp3107;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=true"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AppSettings": {
    "PageSize": 10,
    "DefaultCulture": "ru-RU",
    "SessionTimeoutMinutes": 30
  }
}
```

🎬 Шаг 11: Запуск и тестирование

Команды для запуска:

```bash
# Восстановление пакетов
dotnet restore

# Создание миграций (если изменяли модели)
dotnet ef migrations add InitialCreate
dotnet ef database update

# Запуск приложения
dotnet run
```

Тестовые сценарии:

1. Вход как администратор (login: admin, password: Password123!)
   · Доступ ко всем страницам
   · Возможность редактирования/удаления
2. Вход как билетер (login: ticket_seller, password: Password123!)
   · Просмотр фильмов и сеансов
   · Просмотр и создание билетов
   · Нет доступа к редактированию
3. Вход как гость
   · Только просмотр фильмов и сеансов
   · Нет доступа к билетам и редактированию

📚 Шаблоны для быстрого создания страниц:

Шаблон для страницы Index:

```csharp
// Быстрый шаблон для Index страницы любой сущности
public class IndexModel : AuthPageModel
{
    public IndexModel(CinemaContext context) : base(context) { }
    
    public IList<Entity> Entities { get; set; }
    
    public async Task OnGetAsync()
    {
        // Проверка прав
        if (HasRole() is IActionResult result) return;
        
        // Получение данных
        Entities = await _context.Entities
            .Include(e => e.RelatedEntity)
            .ToListAsync();
    }
}
```

Шаблон для страницы Create/Edit:

```csharp
public class CreateModel : AuthPageModel
{
    [BindProperty]
    public Entity Entity { get; set; }
    
    public SelectList RelatedEntities { get; set; }
    
    public CreateModel(CinemaContext context) : base(context) { }
    
    public async Task<IActionResult> OnGetAsync()
    {
        // Проверка прав на редактирование
        if (CanEdit() is IActionResult result) return result;
        
        // Загрузка данных для dropdown
        await LoadDropdownData();
        return Page();
    }
    
    public async Task<IActionResult> OnPostAsync()
    {
        if (!ModelState.IsValid)
        {
            await LoadDropdownData();
            return Page();
        }
        
        _context.Entities.Add(Entity);
        await _context.SaveChangesAsync();
        
        return RedirectToPage("./Index");
    }
    
    private async Task LoadDropdownData()
    {
        var data = await _context.RelatedEntities.ToListAsync();
        RelatedEntities = new SelectList(data, "Id", "Name");
    }
}
```

🎯 Ключевые моменты, которые нельзя упустить:

1. Добавьте таблицу Users - без нее не будет аутентификации
2. Используйте правильные связи в моделях (Include для связанных данных)
3. Не забывайте про валидацию - аннотации [Required], [Range], [StringLength]
4. Настройте локализацию в Program.cs для корректного отображения дат и чисел
5. Проверяйте права доступа в каждом методе OnGet/OnPost
6. Используйте Session для хранения данных пользователя
7. Добавьте пагинацию для больших списков данных
8. Настройте поиск и фильтрацию через параметры GET-запроса

Это полное руководство для создания проекта, объединяющего все три лабораторные работы. Начните с создания таблицы Users, затем генерируйте модели из БД, и постепенно реализуйте все страницы согласно требованиям лабораторных работ.
